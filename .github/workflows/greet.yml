name: greet
on:
  push:
    branches:
      - newFeature2
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: Greet the world
        run: echo "Hello, World!"
